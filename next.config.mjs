/**
 * @type {import('next').NextConfig}
 */
const nextConfig = {
    // output: 'export',
    // reactStrictMode: true,
    // distDir: "build",
    // output: "export"
  }
   
  module.exports = nextConfig